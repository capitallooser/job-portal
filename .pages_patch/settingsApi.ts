import{supabase}from'../../lib/supabase';import{getCurrentUserId}from'../auth/sessionManager';export async function listSettings(){const{data,error}=await supabase.from('platform_settings').select('*');if(error)throw error;return Object.fromEntries((data??[]).map((x:any)=>[x.key,x.value]))}export async function saveSetting(key:string,value:unknown){const userId=getCurrentUserId();if(!userId)throw new Error('Sign in required');const{error}=await supabase.from('platform_settings').upsert({key,value,updated_by:userId,updated_at:new Date().toISOString()});if(error)throw error}
